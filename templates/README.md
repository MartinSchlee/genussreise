# Genussreise Onboarding
1. venv erstellen & `pip install -r requirements.txt`
2. Ordner `static/uploads/` anlegen + `default.jpg` einfügen.
3. `python app.py` starten. 
Kategorien werden automatisch erstellt. Admin-Key: GEHEIM123.